import 'package:flutter/material.dart';

class Computerassisteddrawing extends StatelessWidget {
  const Computerassisteddrawing({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Computer Assisted Drawing'),
      ),
    );
  }
}