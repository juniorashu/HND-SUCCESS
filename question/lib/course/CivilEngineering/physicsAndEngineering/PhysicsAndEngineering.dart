import 'package:flutter/material.dart';

class Physicsandengineering extends StatelessWidget {
  const Physicsandengineering({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('physics and engineering'),
      ),
    );
  }
}