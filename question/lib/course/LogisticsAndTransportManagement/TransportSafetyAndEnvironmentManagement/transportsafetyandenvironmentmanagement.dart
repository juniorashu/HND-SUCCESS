import 'package:flutter/material.dart';

class Transportsafetyandenvironmentmanagement extends StatelessWidget {
  const Transportsafetyandenvironmentmanagement({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('transport safety and environment management'),
      ),
    );
  }
}
