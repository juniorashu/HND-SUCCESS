import 'package:flutter/material.dart';

class Casestudylogitics extends StatelessWidget {
  const Casestudylogitics({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Case Study '),
      ),
    );
  }
}