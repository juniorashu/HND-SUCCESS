import 'package:flutter/material.dart';

class Transportlaw extends StatelessWidget {
  const Transportlaw({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Transport Law'),
      ),
    );
  }
}