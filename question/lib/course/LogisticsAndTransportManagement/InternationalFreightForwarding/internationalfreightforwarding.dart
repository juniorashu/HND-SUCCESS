import 'package:flutter/material.dart';

class Internationalfreightforwarding extends StatelessWidget {
  const Internationalfreightforwarding({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title:const Text('INternational Freight Forwarding'),
      ),
    );
  }
}