import 'package:flutter/material.dart';

class Casestudymechanical extends StatelessWidget {
  const Casestudymechanical({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Case study'),
      ),
    );
  }
}