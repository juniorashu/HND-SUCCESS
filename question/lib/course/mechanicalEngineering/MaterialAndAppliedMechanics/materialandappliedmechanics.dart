import 'package:flutter/material.dart';

class Materialandappliedmechanics extends StatelessWidget {
  const Materialandappliedmechanics({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Material and applied mechanics'),),
    );
  }
}