import 'package:flutter/material.dart';

class MaintainanceAndAutomation extends StatelessWidget {
  const MaintainanceAndAutomation({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title:const  Text('Maintainance And Automation'),),
    );
  }
}