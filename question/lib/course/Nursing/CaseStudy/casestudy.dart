import 'package:flutter/material.dart';

class Casestudynursing extends StatelessWidget {
  const Casestudynursing({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Case Study'),
      ),
    );
  }
}