import 'package:flutter/material.dart';

class Nursingsscience extends StatelessWidget {
  const Nursingsscience({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Nursing science'),
      ),
    );
  }
}