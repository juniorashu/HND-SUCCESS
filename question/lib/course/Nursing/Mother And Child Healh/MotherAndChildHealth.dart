import 'package:flutter/material.dart';

class Motherandchildhealth extends StatelessWidget {
  const Motherandchildhealth({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Mother And Child Health'),
      ),
    );
  }
}