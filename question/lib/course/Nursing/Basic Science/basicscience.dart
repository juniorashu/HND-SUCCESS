import 'package:flutter/material.dart';

class Basicscience extends StatelessWidget {
  const Basicscience({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Basic Sceince'),
      ),
    );
  }
}