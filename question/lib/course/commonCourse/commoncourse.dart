import 'package:flutter/material.dart';

class Commoncourse extends StatelessWidget {
  const Commoncourse({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title:const  Text('common course'),
      ),
    );
  }
}