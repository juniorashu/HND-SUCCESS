import 'package:flutter/material.dart';

class Electroniccirciut extends StatelessWidget {
  const Electroniccirciut({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Electronic circuit'),
      ),
    );
  }
}
