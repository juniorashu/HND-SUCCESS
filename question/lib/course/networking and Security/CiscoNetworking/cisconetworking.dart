import 'package:flutter/material.dart';

class Cisconetworking extends StatelessWidget {
  const Cisconetworking({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Cisco network'),
      ),
    );
  }
}