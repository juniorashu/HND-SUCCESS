import 'package:flutter/material.dart';

class Informationsecurity extends StatelessWidget {
  const Informationsecurity({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Information Security'),
      ),
    );
  }
}