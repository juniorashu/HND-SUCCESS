import 'package:flutter/material.dart';

class Telephonyandswitching extends StatelessWidget {
  const Telephonyandswitching({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Telephony and switching'),
      ),
    );
  }
}