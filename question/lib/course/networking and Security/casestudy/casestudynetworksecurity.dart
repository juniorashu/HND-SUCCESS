import 'package:flutter/material.dart';

class Casestudynetworksecurity extends StatelessWidget {
  const Casestudynetworksecurity({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Case study'),
      ),
    ); 
  }
}