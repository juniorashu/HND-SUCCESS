import 'package:flutter/material.dart';

class Computertechnology extends StatelessWidget {
  const Computertechnology({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title:const Text('Computer Technology'),),
    );
  }
}