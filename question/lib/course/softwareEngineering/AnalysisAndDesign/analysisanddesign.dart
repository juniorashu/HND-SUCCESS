import 'package:flutter/material.dart';

class Analysisanddesign extends StatelessWidget {
  const Analysisanddesign({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title:const  Text('Analysis and Design'),
      ),
    );
  }
}