import 'package:flutter/material.dart';

class Digitalliteracy extends StatelessWidget {
  const Digitalliteracy({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Digital Literacy'),
      ),
    );
  }
}