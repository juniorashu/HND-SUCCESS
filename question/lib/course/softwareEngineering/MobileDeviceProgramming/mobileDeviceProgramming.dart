import 'package:flutter/material.dart';

class Mobiledeviceprogramming extends StatelessWidget {
  const Mobiledeviceprogramming({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title:const  Text('Mobile device programming questions'),
      ),
    );
  }
}
