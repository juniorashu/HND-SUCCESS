import 'package:flutter/material.dart';

class Technicalcommunication extends StatelessWidget {
  const Technicalcommunication({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Technical Communication'),
      ),
    ) ;
  }
}