import 'package:flutter/material.dart';

class Digitalmakerting extends StatelessWidget {
  const Digitalmakerting({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('digital makerting'),
      ),
    );
  }
}