import 'package:flutter/material.dart';

class Computerstudies extends StatelessWidget {
  const Computerstudies({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Computer studies'),
      ),
    );
  }
}