import 'package:flutter/material.dart';

class Casestudyecommerce extends StatelessWidget {
  const Casestudyecommerce({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title:const Text('Case Study ecommerce'),
      ),
    );
  }
}